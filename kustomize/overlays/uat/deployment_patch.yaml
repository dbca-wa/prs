apiVersion: apps/v1
kind: Deployment
metadata:
  name: prs-deployment
spec:
  template:
    spec:
      containers:
        - name: prs
          envFrom:
            - secretRef:
                name: prs-env-uat
